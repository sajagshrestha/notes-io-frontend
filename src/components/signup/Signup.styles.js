import styled from "styled-components";
import { HomeWrapper, HeroImg } from "../home/Home.styles";

export const SignupWrapper = styled(HomeWrapper)``;

export const SignupImg = styled(HeroImg)`
	width: 35rem;
`;
